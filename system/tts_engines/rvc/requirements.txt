# RVC (Retrieval-based Voice Conversion) engine dependencies
#
# INSTALL STEPS:
#   1. On macOS, install MeCab (needed by fugashi):
#        brew install mecab
#
#   2. Then install the rest:
#        pip install -r system/tts_engines/rvc/requirements.txt

# Audio processing
librosa>=0.10.2
praat-parselmouth>=0.4.4
pyworld>=0.3.4
soundfile>=0.12.1
torchaudio
noisereduce
ffmpeg-python>=0.2.0

# Machine learning
torch
numpy
scipy
einops
local_attention==1.9.10
torchcrepe>=0.0.2

# Search / indexing
faiss-cpu

# Utilities
requests
tqdm
beautifulsoup4
wget
six
matplotlib


# Japanese text support (for RVC training/inference with Japanese)
# Requires MeCab: brew install mecab (macOS) or apt install libmecab-dev (Linux)
fugashi==1.3.1
